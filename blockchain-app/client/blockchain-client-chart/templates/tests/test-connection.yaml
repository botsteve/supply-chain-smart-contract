apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "blockchain-client-chart.fullname" . }}-test-connection"
  labels:
    {{- include "blockchain-client-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "blockchain-client-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
